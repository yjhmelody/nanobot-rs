# nanobot-rs

## Development

This repository includes a `justfile` with common development tasks.

```bash
just                 # list available tasks
just ci              # run local CI checks
just agent -m "hi"   # run agent mode
```
